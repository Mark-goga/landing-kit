{
  "name": "@mark-goga/landing-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared Astro landing chrome (components, layouts, blog pipeline, orval-generated MindAI client) consumed by Fluxo, Dzing, and future landings as a git submodule.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "contracts:generate": "orval --config orval.config.ts",
    "contracts:check": "npm run contracts:generate && git diff --exit-code -- src/api/generated",
    "contracts:sync": "node src/scripts/pull-mindai-spec.mjs",
    "test": "node --test src/scripts/*.test.mjs"
  },
  "peerDependencies": {
    "astro": "^6.4.2"
  },
  "dependencies": {
    "yaml": "^2.5.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^6.4.2",
    "orval": "^7.14.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
